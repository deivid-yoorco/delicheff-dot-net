@model Nop.Web.Areas.Admin.Models.Rewards.LevelModel

@{
    //page title
    ViewBag.Title = "Agregar nivel";
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Levels");
}

<form asp-controller="Levels" asp-action="Create" method="post" id="level-form">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            Agregar nivel
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="List">regresar a la lista de niveles</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>