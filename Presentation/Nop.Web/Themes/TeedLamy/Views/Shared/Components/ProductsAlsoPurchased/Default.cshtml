@model IList<ProductOverviewModel>

@if (Model.Count > 0)
{
    <div class="section margin-mobile">
        <h5>@T("Products.AlsoPurchased")</h5>

        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col s6 m6">
                    @await Html.PartialAsync("_ProductBox", item)
                </div>
            }
        </div>
    </div>
}