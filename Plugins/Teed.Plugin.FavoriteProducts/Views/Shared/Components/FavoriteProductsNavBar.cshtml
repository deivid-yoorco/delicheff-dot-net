@model Teed.Plugin.FavoriteProducts.Models.ConfigurationModel

@if (Model.Active)
{
    if (string.IsNullOrEmpty(Model.DivElementClassString))
    {
        <li class="collection-item"><a href="/FavoriteProducts">@(Model.TextMenu)</a></li>
    }
    else
    {
        <div class="@Model.DivElementClassString">
            <a href="/FavoriteProducts">@(Model.TextMenu)</a>
        </div>
    }
}